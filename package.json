{
  "name": "youbuy-capacitor",
  "version": "1.0.0",
  "description": "Native Wrapper for youbuy-shopies",
  "main": "index.js",
  "scripts": {
    "start": "npx cap serve",
    "build": "echo 'No build step needed for wrapper'",
    "sync": "npx cap sync",
    "add-android": "npx cap add android",
    "open-android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
